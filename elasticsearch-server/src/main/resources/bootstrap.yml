spring:
  application:
    name: cache-service
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      profile: dev
  rabbitmq:
    host: 106.13.36.82
    port: 5672
    username: gosun
    password: gosun

management:
  endpoint:
    web:
      exposure:
        include: "*"